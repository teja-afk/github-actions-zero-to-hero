---
name: Check Tools

on:
  push:

jobs:
  tools:
    runs-on: ubuntu-latest

    steps:
      - name: Check version
        run: |
          docker --version
          python3 --version
          node --version
          git --version
