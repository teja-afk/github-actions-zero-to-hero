---
name: PR Check

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize

jobs:
  pr-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print branch name
        run: echo "PR check running for branch: ${{ github.head_ref }}"
